# content/sources/

Drop each week's course material here (`week-NN/` subfolders: PPTX, PDF, markdown notes, case excerpts) as grounding input for `npm run generate`.

Everything in this directory except this README is **gitignored** — publisher and case content is copyrighted and must never enter the repository.

---
layout: layout.njk
pagination:
  data: bank.weeks
  size: 1
  alias: w
permalink: "/week-{{ w.week }}/"
eleventyComputed:
  title: "Week {{ w.week }} — {{ w.title }}"
---
<nav class="crumb mono"><a href="/">← MISSION SELECT</a></nav>
<section class="hero hero-quiz">
  <p class="kicker mono">WEEK {{ w.week | pad2 }} · {{ w.questions.length }} QUESTIONS</p>
  <h1>{{ w.title }}</h1>
</section>

<div id="quiz" data-mode="week" data-week="{{ w.week }}">
  <div class="quiz-panel">
    <p class="readout mono">CONNECTING…</p>
    <p class="done-note">If this message doesn't clear in a few seconds, PAL couldn't load — check your connection and reload.</p>
  </div>
</div>

<script type="application/json" id="question-data">{{ w.questions | embedJson | safe }}</script>
<script type="module" src="/assets/js/week-page.js"></script>
